# Project Setup Instructions

This project uses cursor rules for initialization. Please follow the instructions in:

`.cursor/rules/77-projects-app-init.mdc`

This rule file contains a comprehensive checklist and code templates for initializing this Next.js application following the 77-projects structure.

## Quick Start

1. Open this project in Cursor
2. Ask Cursor to: "Please follow the instructions in .cursor/rules/77-projects-app-init.mdc to set up this project"
3. Cursor will read the rule file and execute all the setup steps automatically

The rule file includes:

- Package configuration
- Configuration files (ESLint, Prettier, TypeScript, etc.)
- Directory structure
- Core application files
- Git hooks setup
- And more...

## Project Name

Project name: `nat-and-ben`

When following the rule, replace `<project-name>` with: `nat-and-ben`
